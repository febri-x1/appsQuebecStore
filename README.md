# Quebec Store

Sistem POS kasir sederhana dengan PHP native dan MySQL.

## Instalasi

1. Buat database `quebec_store`.
2. Import `database/schema.sql`, lalu `database/seeder.sql`.
3. Sesuaikan konfigurasi database di `config/database.php`.
4. Buka `http://localhost/quebecStore`.
